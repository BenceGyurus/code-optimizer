from typing import List





def compute_001(values: List[int]) -> List[int]:
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
        if v not in result:
            result.append(v)
    return result
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
        if v not in result:
            result.append(v)
    return result
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
        if v not in result:
            result.append(v)
    return result
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
        if v not in result:
            result.append(v)
    return result
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
        if v not in result:
            result.append(v)
    return result
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
        if v not in result:
            result.append(v)
    return result
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


def compute_014(values: List[int]) -> List[int]:
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


def compute_015(values: List[int]) -> List[int]:
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


def compute_016(values: List[int]) -> List[int]:
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


def compute_017(values: List[int]) -> List[int]:
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


def compute_018(values: List[int]) -> List[int]:
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


def compute_019(values: List[int]) -> List[int]:
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


def compute_020(values: List[int]) -> List[int]:
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


def compute_021(values: List[int]) -> List[int]:
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


def compute_022(values: List[int]) -> List[int]:
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


def compute_023(values: List[int]) -> List[int]:
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


def compute_024(values: List[int]) -> List[int]:
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


def compute_025(values: List[int]) -> List[int]:
    result = []
    result_seen = set()
    for v in values:
        if v not in result_seen:
            result.append(v)
            result_seen.add(v)
    return result


def compute_026(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_027(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_028(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_029(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_030(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_031(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_032(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_033(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_034(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_035(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_036(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_037(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_038(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_039(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_040(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_041(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_042(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_043(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_044(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_045(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_046(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_047(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_048(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_049(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_050(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_051(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_052(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_053(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_054(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_055(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_056(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_057(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_058(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_059(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_060(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_061(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_062(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_063(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_064(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_065(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_066(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_067(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_068(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_069(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_070(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_071(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_072(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_073(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_074(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_075(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_076(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_077(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_078(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_079(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_080(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_081(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_082(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_083(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_084(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_085(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_086(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_087(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_088(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_089(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_090(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_091(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_092(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_093(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_094(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_095(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_096(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_097(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_098(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_099(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_100(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_101(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_102(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_103(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_104(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_105(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_106(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_107(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_108(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_109(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_110(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_111(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_112(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_113(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_114(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_115(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_116(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_117(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_118(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_119(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_120(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_121(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_122(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_123(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_124(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_125(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_126(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_127(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_128(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_129(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_130(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_131(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_132(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_133(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_134(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_135(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_136(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_137(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_138(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_139(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_140(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_141(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_142(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_143(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_144(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_145(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_146(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_147(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_148(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_149(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result


def compute_150(values: List[int]) -> List[int]:
    result = []
    for v in values:
        if v not in result:
            result.append(v)
    return result
